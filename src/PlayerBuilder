// builder class: outline the methods needed for creating/building a Player object
public interface PlayerBuilder {
    PlayerBuilder buildFirstName(String firstName);

    PlayerBuilder buildPassword(String playerPass);

    Player build();
}
